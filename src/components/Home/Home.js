import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <h2>This is the real home components</h2>
        </div>
    );
};

export default Home;